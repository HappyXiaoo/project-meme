# 实验记录

这份文档用于记录每次模型改动、指标结果和实验结论，后续可以直接整理进论文第 4 章“实验与结果分析”。

## 记录规范

每次实验至少记录以下内容：

- 日期
- 实验名称
- 实验目的
- 模型方案
- 数据来源
- 数据规模
- 评价指标
- 关键改动
- 结果结论
- 下一步计划

建议顺序：

- 先记录基线模型
- 再记录模因式表达特征模型
- 最后记录 BERT 版，并区分 `CPU smoke test` 和 `GPU 正式训练`

---

## 实验 01：基线系统与基线模型

- 日期：2026-04-17
- 实验名称：基线系统搭建与基础文本分类模型
- 实验目的：构建一个可运行、可训练、可保存、可加载、可预测的毕业设计基础版本，为后续“模因式表达特征”和 `BERT` 模型实验提供对比基线。
- 系统实现范围：
  - 完成前端页面原型搭建
  - 完成后端接口开发
  - 完成前后端联调
  - 完成基线模型训练、保存与加载
- 模型方案：`TF-IDF + Logistic Regression`
- 数据来源：[data/data.csv](/d:/code/project-meme/data/data.csv)
- 训练脚本：[train_model.py](/d:/code/project-meme/back-end/scripts/train_model.py)
- 模型服务：[classifier.py](/d:/code/project-meme/back-end/app/services/classifier.py)
- 模型产物目录：`back-end/artifacts/`
- 数据规模：
  - 总样本数：13471
  - 训练集：10776
  - 测试集：2695
  - 标签分布：`0 -> 7904`，`1 -> 5567`
- 评价指标：
  - accuracy：0.8223
  - precision：0.7754
  - recall：0.8025
  - f1：0.7887
- 当前实现说明：
  - 使用中文字符级 `TF-IDF` 提取文本特征
  - 使用 `Logistic Regression` 完成二分类
  - 模型训练完成后保存为 `model.joblib`
  - 同步保存 `metrics.json` 和 `metadata.json`
  - 后端启动时优先加载已训练模型
  - 对明显攻击性文本叠加了风险关键词增强策略，以提升演示稳定性
- 阶段成果：
  - 系统已经具备完整的输入、预测、展示流程
  - 后端可以通过接口返回真实预测结果
  - 当前模型已经可以作为后续改进实验的正式基线
- 当前不足：
  - 当前模型更接近“有害文本识别”，与“模因识别”的联系还不够强
  - 还没有引入模板化表达、群体指向、情绪强化等模因式表达特征
  - 还没有与预训练模型进行对比
- 当前结论：
  - 基线系统已经搭建完成
  - 基线模型指标可接受，适合作为后续改进实验的对照组
  - 下一阶段应重点补充“模因式表达特征”，提高系统与课题题目的贴合度
- 下一步计划：
  - 增加模板化表达、群体指向、情绪强化、嘲讽表达等特征
  - 对比加入模因式表达特征前后的指标变化
  - 尝试 `BERT` 或 `RoBERTa` 作为进一步优化方案

---

## 实验 02：模因式表达特征

- 日期：2026-04-17
- 实验名称：模因式表达特征增强模型
- 实验目的：在基线文本分类模型的基础上，引入模板化表达、群体指向、情绪强化、嘲讽表达等特征，使模型更贴近“文本模因识别”场景，并观察其对整体指标和典型样例的影响。
- 模型方案：`TF-IDF + Meme Expression Features + Logistic Regression`
- 数据来源：[data/data.csv](/d:/code/project-meme/data/data.csv)
- 训练脚本：[train_model.py](/d:/code/project-meme/back-end/scripts/train_model.py)
- 实现位置：[classifier.py](/d:/code/project-meme/back-end/app/services/classifier.py)
- 数据规模：
  - 总样本数：13471
  - 训练集：10776
  - 测试集：2695
- 评价指标：
  - accuracy：0.8219
  - precision：0.7861
  - recall：0.7819
  - f1：0.7840
- 关键改动：
  - 保留基线的字符级 `TF-IDF` 特征
  - 新增模因式表达特征提取器
  - 将人工特征与 `TF-IDF` 特征拼接后输入 `Logistic Regression`
  - 当前加入的特征包括：
    - 模板化表达特征
    - 群体指向特征
    - 情绪强化特征
    - 嘲讽表达特征
    - 对立结构特征
    - 模因短语特征
    - 标点爆发特征
    - 重复字符特征
    - 风险词与群体词共现特征
- 样例观察：
  - 对样例“不是我说，某些人真的是典中典。”，基线模型判为正常文本，改进模型判为疑似有害文本
  - 说明加入模因式表达特征后，模型对模板化、网络化表达的敏感度有所提升
  - 但对“我反对任何形式的歧视言论。”这类含敏感词但语义并非攻击的文本，误判问题仍然存在
- 当前结论：
  - 从整体指标看，`meme_features_v1` 暂时没有超过基线模型
  - 从典型样例看，改进模型已经开始捕捉部分模因式表达模式
  - 这说明模因式表达特征是有效方向，但当前特征设计仍需继续优化
- 下一步计划：
  - 继续扩充模板化表达和网络梗表达特征
  - 优化对“描述性语句”和“反对性语句”的误判问题
  - 尝试引入 `BERT` 进行语义层面对比

### 实验 02 的继续优化：语境消歧

- 日期：2026-04-17
- 优化目的：修正“反对歧视”“描述侮辱行为”这类文本被敏感词规则误判的问题。
- 关键改动：
  - 增加“反对/缓和语境”词特征，如“反对”“抵制”“谴责”“避免”等
  - 增加“描述/引用语境”词特征，如“言论”“说法”“行为”“审核”“讨论”等
  - 增加“分析语境”词特征，如“像是”“带有”“属于”“涉嫌”等
  - 将后处理逻辑拆成“直接攻击语境”“模因式攻击语境”“描述/反对语境”三类，而不是只要命中敏感词就直接判为风险文本
- 优化后指标：
  - accuracy：0.8200
  - precision：0.7831
  - recall：0.7810
  - f1：0.7820
- 样例观察：
  - “我反对任何形式的歧视言论。”：优化后由风险文本修正为正常文本
  - “这种说法像是在侮辱别人。”：优化后由风险文本修正为正常文本
  - “不是我说，某些人真的是典中典。”：优化后仍可识别为疑似有害文本
  - “果然某些人又开始这一套了，笑死。”：优化后可以识别为更隐含的模因式攻击表达
- 当前结论：
  - 总体指标略有下降，但目标场景下的误判修复明显
  - 当前优化更强调“课题贴合度”和“样例解释力”，而不是只追求总体分数
  - 这一阶段的变化非常适合写进论文中的实验分析部分
- 下一步计划：
  - 继续细化语境规则，防止修复误判的同时引入新的漏判
  - 记录更多模因式表达样例
  - 进入 `BERT` 对比实验阶段

---

## 实验 03：BERT 初版

- 日期：
- 实验名称：BERT 文本分类模型初版
- 实验目的：在传统机器学习模型基础上，引入预训练语言模型 `BERT`，比较其在整体指标、典型样例和语义理解能力上的表现差异。
- 模型方案：`bert-base-chinese` 或其他中文预训练模型
- 数据来源：[data/data.csv](/d:/code/project-meme/data/data.csv)
- 训练脚本：[train_bert.py](/d:/code/project-meme/back-end/scripts/train_bert.py)
- 配置文件：[config.py](/d:/code/project-meme/back-end/bert/config.py)
- 训练流程：[train.py](/d:/code/project-meme/back-end/bert/train.py)
- 数据处理：[data.py](/d:/code/project-meme/back-end/bert/data.py)
- 数据规模：
  - 总样本数：
  - 训练集：
  - 验证集：
  - 测试集：
- 训练环境：
  - 设备：CPU / GPU
  - Python 版本：
  - torch 版本：
  - transformers 版本：
- 训练参数：
  - model_name：
  - max_length：
  - learning_rate：
  - num_train_epochs：
  - train_batch_size：
  - eval_batch_size：
  - gradient_accumulation_steps：
  - smoke_test：是 / 否
- 评价指标：
  - accuracy：
  - precision：
  - recall：
  - f1：
- 关键改动：
  - 引入 tokenizer，将中文文本转换为 `input_ids` 和 `attention_mask`
  - 使用 `AutoModelForSequenceClassification` 完成二分类微调
  - 保存模型、tokenizer、metrics 和 metadata
- 样例观察：
  - 典型正常文本：
  - 典型风险文本：
  - 典型模因式表达文本：
  - 容易误判文本：
- 当前结论：
- 与前两版模型对比：
  - 与基线模型相比：
  - 与 `meme_features_v1` 相比：
  - 在典型样例上的优势：
  - 当前存在的问题：
- 下一步计划：

### 实验 03 的子记录建议

为了让 BERT 版实验过程更清晰，建议继续拆成两个子阶段记录。

#### 03-A：CPU smoke test

- 日期：
- 目的：只验证 BERT 训练代码、数据读取、tokenizer、模型保存流程是否能跑通
- 建议参数：
  - `--smoke-test`
  - `--cpu-only`
  - `--train-sample-limit 128`
  - `--eval-sample-limit 64`
- 需要记录：
  - 是否成功启动训练
  - 是否成功保存模型和 tokenizer
  - 是否成功输出 `metrics.json` 和 `metadata.json`
  - 单轮训练耗时大约多少
- 结论模板：
  - “本次实验主要用于验证 BERT 版训练流程是否可运行，不作为正式对比结果。”

#### 03-A 实际记录：GPU smoke test

- 日期：2026-04-18
- 实验名称：BERT 小样本 GPU smoke test
- 实验目的：验证 `bert-base-chinese` 在 GPU 环境下的训练链路是否完整可用，包括模型下载、tokenizer 加载、训练、评估、模型保存和指标输出。
- 运行环境：
  - 设备：GPU
  - GPU：RTX 3090 24GB
  - Python：3.10.8
  - torch：2.4.1
  - transformers：4.46.3
  - accelerate：1.1.1
- 模型方案：`bert-base-chinese`
- 训练命令：
  - `export HF_ENDPOINT=https://hf-mirror.com`
  - `python scripts/train_bert.py --smoke-test --train-sample-limit 128 --eval-sample-limit 64`
- 数据规模：
  - 训练集：128
  - 验证集：64
  - 测试集：64
- 训练参数：
  - max_length：128
  - learning_rate：2e-5
  - num_train_epochs：2
  - train_batch_size：4
  - eval_batch_size：8
  - gradient_accumulation_steps：1
  - smoke_test：是
- 评价指标：
  - accuracy：0.7656
  - precision：0.7391
  - recall：0.6538
  - f1：0.6939
- 训练现象：
  - Hugging Face 官方地址不可达，但通过 `HF_ENDPOINT=https://hf-mirror.com` 成功完成模型下载与训练
  - BERT 分类头参数为新初始化，属于正常现象
  - 训练、验证、测试、模型保存流程全部跑通
- 当前结论：
  - BERT 版训练链路已经打通
  - GPU 环境可正常用于后续正式训练
  - 当前结果仅用于 smoke test 验证，不作为论文正式对比实验结果
- 下一步计划：
  - 使用完整数据集进行正式训练
  - 保存正式训练结果并与基线模型、模因特征模型做对比
  - 将正式训练结果回填到测试样例对比表中

#### 03-B：GPU 正式训练

- 日期：2026-04-18
- 目的：在完整数据集上正式训练 BERT 模型，用于与基线模型、模因特征模型做论文对比。
- 运行环境：
  - 设备：GPU
  - GPU：RTX 3090 24GB
  - Python：3.10.8
  - 模型：`bert-base-chinese`
- 训练参数：
  - max_length：128
  - learning_rate：2e-5
  - num_train_epochs：2
  - train_batch_size：8
  - eval_batch_size：8
  - gradient_accumulation_steps：1
- 数据规模：
  - 训练集：10776
  - 验证集：1347
  - 测试集：1348
- 最终测试集指标：
  - accuracy：0.8887
  - precision：0.8515
  - recall：0.8851
  - f1：0.8680
- 结果分析：
  - BERT 正式训练结果显著高于基线模型 `TF-IDF + Logistic Regression`
  - BERT 正式训练结果也高于当前 `meme_features_v1`
  - 说明预训练语言模型在中文文本语义理解方面具备明显优势
- 当前结论：
  - 本次实验可以作为论文中的正式对比实验结果
  - BERT 已成为当前项目中指标表现最好的模型方案
  - 后续可以补充典型样例预测结果，增强论文中的案例分析部分
- 下一步计划：
  - 回填测试样例中的 BERT 结果
  - 做三模型对比总结
  - 将结果整理进论文第 4 章“实验与结果分析”

### 实验 03 的补充分析：人工测试集三模型对比

- 日期：2026-04-18
- 实验目的：使用人工构造测试集进一步观察三种模型在目标场景中的表现差异，尤其关注模因式攻击表达和隐含群体攻击表达。
- 使用数据集：[manual_test_set.csv](/d:/code/project-meme/data/manual_test_set.csv)
- 对比模型：
  - 基线模型 `TF-IDF + Logistic Regression`
  - `meme_features_v1`
  - `BERT`
- 总体准确率：
  - baseline：0.7667
  - meme_features_v1：0.7333
  - bert：0.6000
- 关键观察：
  - BERT 在正式测试集上的总体指标最好，但在人工构造的模因式风险样例上明显下降
  - 在 `meme_harm` 和 `implicit_group_attack` 两类样例上，BERT 表现最弱
  - `meme_features_v1` 虽然总体准确率不是最高，但在课题相关的模因式表达场景上更有针对性
  - 这说明“总体测试集性能”和“人工目标场景性能”并不完全一致
- 当前结论：
  - BERT 更适合做总体语义分类基线
  - 模因特征模型更适合体现课题本身的场景特征
  - 论文中应同时呈现“正式测试集结果”和“人工目标场景测试集结果”，形成更完整的实验分析
